name: Secret Masker Test

# Step 3: Trigger manually using workflow_dispatch
on:
  workflow_dispatch:

jobs:
  test-secrets:
    runs-on: ubuntu-latest
    steps:
      # Step 4: Attempt to echo the secret to the log
      - name: Reveal the Secret
        run: echo "The highly sensitive deployment token is ${{ secrets.MY_SENSITIVE_TOKEN }}"
